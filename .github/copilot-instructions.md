# Copilot Instructions

## Project Guidelines
- Always add log_write_boot() debug logging when implementing new features, so bugs can be diagnosed from sphaira_boot.log without needing the optional log setting enabled. The user explicitly requires this for every new feature.